class Player {
  constructor(id, wins, hand) {
    this.id = id;
    this.wins = 0;
    this.hand = hand || [];
  }

  playCard() {
    //take last card in array to push to centralPile in game.js
  }

  saveWinsToStorage() {

  }
}
//how to push from game class to player class?
